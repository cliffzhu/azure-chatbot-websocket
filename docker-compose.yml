version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: azure-chatbot-websocket:local
    env_file:
      - .env
    ports:
      - "3978:3978"
    restart: unless-stopped
